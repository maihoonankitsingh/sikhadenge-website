import Link from "next/link";
import generatedPages from "../../data/generated-seo-merged.json";
import { Search, MapPin, Briefcase, GraduationCap } from "lucide-react";

export const metadata = {
  title: "Professional Experts Directory | Sikhadenge",
  description: "Find the best AI and Digital Experts specialized in your industry and city.",
};

export default function ExpertsDirectory() {
  // Unique Categories and Cities nikalne ke liye logic
  const cities = Array.from(new Set(generatedPages.map((p: any) => p.city))).sort();
  const skills = Array.from(new Set(generatedPages.map((p: any) => p.skill))).sort();

  return (
    <main className="min-h-screen bg-white text-slate-900 font-sans">
      {/* Header Section */}
      <section className="bg-slate-50 border-b border-slate-200 pt-24 pb-16">
        <div className="mx-auto max-w-7xl px-4 text-center">
          <h1 className="text-4xl font-black mb-4 sm:text-6xl">Expert <span className="text-blue-600">Directory</span></h1>
          <p className="text-slate-600 text-lg max-w-2xl mx-auto mb-10">
            Browse through our network of 4,600+ specialized experts across India. Pick your skill, industry, and city to get started.
          </p>

          {/* Mock Search Bar UI */}
          <div className="max-w-2xl mx-auto relative">
            <div className="absolute inset-y-0 left-4 flex items-center pointer-events-none">
              <Search className="h-5 w-5 text-slate-400" />
            </div>
            <input
              type="text"
              placeholder="Search by skill or city (e.g. Python Mumbai)..."
              className="w-full pl-12 pr-4 py-4 rounded-2xl border border-slate-200 shadow-sm focus:ring-2 focus:ring-blue-500 outline-none transition-all"
            />
          </div>
        </div>
      </section>

      {/* Directory Grid */}
      <section className="py-20 mx-auto max-w-7xl px-4 grid md:grid-cols-2 gap-16">

        {/* Skills Column */}
        <div>
          <div className="flex items-center mb-8">
            <GraduationCap className="w-6 h-6 text-blue-600 mr-3" />
            <h2 className="text-2xl font-bold">Skills / Categories</h2>
          </div>
          <div className="grid grid-cols-1 gap-3">
            {skills.map((skill, i) => (
              <Link
                key={i}
                href={`/expert/${skill.toLowerCase().replace(/ /g, '-')}-for-healthcare-in-remote`} // Link to a default starter page
                className="px-6 py-4 rounded-xl border border-slate-100 bg-slate-50 hover:bg-white hover:border-blue-200 hover:text-blue-600 transition-all font-semibold shadow-sm"
              >
                {skill}
              </Link>
            ))}
          </div>
        </div>

        {/* Cities Column */}
        <div>
          <div className="flex items-center mb-8">
            <MapPin className="w-6 h-6 text-blue-600 mr-3" />
            <h2 className="text-2xl font-bold">Top Locations</h2>
          </div>
          <div className="grid grid-cols-2 gap-3 text-sm">
            {cities.map((city, i) => (
              <Link
                key={i}
                href="#"
                className="px-5 py-3 rounded-xl border border-slate-100 bg-slate-50 hover:bg-white hover:text-blue-600 transition-all text-slate-600"
              >
                {city}
              </Link>
            ))}
          </div>
        </div>

      </section>

      {/* Stats Section */}
      <section className="py-20 bg-blue-600 text-white text-center">
        <div className="grid grid-cols-1 sm:grid-cols-3 gap-10 max-w-4xl mx-auto">
          <div><div className="text-5xl font-black mb-2">20+</div><div className="text-blue-100 uppercase font-bold tracking-widest text-xs">Categories</div></div>
          <div><div className="text-5xl font-black mb-2">20+</div><div className="text-blue-100 uppercase font-bold tracking-widest text-xs">Cities</div></div>
          <div><div className="text-5xl font-black mb-2">10+</div><div className="text-blue-100 uppercase font-bold tracking-widest text-xs">Industries</div></div>
        </div>
      </section>

    </main>
  );
}

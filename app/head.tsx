export default function Head() {
  return (
    <>
      {/* Primary SEO metadata is managed via app/layout.tsx and per-page metadata exports. */}
      <meta name="format-detection" content="telephone=no, date=no, email=no, address=no" />
    </>
  );
}

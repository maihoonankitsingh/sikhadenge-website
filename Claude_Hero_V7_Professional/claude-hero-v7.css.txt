/* CLAUDE HERO V7 PROFESSIONAL START
   Research-backed first-screen redesign.
   Uses existing live HTML/CSS-module DOM. No hero image asset required.
*/

.hero{
  position:relative;
  overflow:hidden;
  padding:clamp(72px,6vw,92px) 0 clamp(64px,5.5vw,82px);
  background:
    radial-gradient(720px 520px at 88% 18%,rgba(218,96,61,.105),transparent 68%),
    radial-gradient(560px 420px at 8% 5%,rgba(245,183,150,.10),transparent 72%),
    linear-gradient(180deg,#fffdfa 0%,#fffaf6 100%);
  border-bottom:1px solid rgba(133,91,67,.10);
}

.hero::before{
  content:"";
  position:absolute;
  inset:0;
  pointer-events:none;
  opacity:.20;
  background-image:
    linear-gradient(rgba(146,101,78,.04) 1px,transparent 1px),
    linear-gradient(90deg,rgba(146,101,78,.04) 1px,transparent 1px);
  background-size:72px 72px;
  -webkit-mask-image:linear-gradient(180deg,#000 0%,rgba(0,0,0,.42) 52%,transparent 92%);
  mask-image:linear-gradient(180deg,#000 0%,rgba(0,0,0,.42) 52%,transparent 92%);
}

.heroGrid{
  position:relative;
  z-index:1;
  min-height:640px;
  display:grid;
  grid-template-columns:minmax(0,.98fr) minmax(480px,1.02fr);
  gap:clamp(48px,5vw,72px);
  align-items:center;
}

.heroGrid > div:first-child{
  max-width:635px;
}

.livePill{
  display:inline-flex;
  align-items:center;
  gap:9px;
  min-height:36px;
  padding:8px 13px;
  border:1px solid rgba(195,74,43,.18);
  border-radius:999px;
  background:rgba(255,255,255,.76);
  color:#a9432b;
  box-shadow:0 10px 28px rgba(83,48,30,.055);
  backdrop-filter:blur(12px);
  font-size:11px;
  font-weight:850;
  letter-spacing:.09em;
  text-transform:uppercase;
}

.livePill i{
  width:8px;
  height:8px;
  border-radius:50%;
  background:#df6744;
  box-shadow:0 0 0 5px rgba(223,103,68,.11);
}

.hero h1{
  max-width:620px;
  margin:24px 0 18px;
  font-size:clamp(58px,5.05vw,78px);
  line-height:.965;
  letter-spacing:-.058em;
  font-weight:900;
  text-wrap:balance;
}

.hero h1 em{
  font-style:normal;
  background:linear-gradient(112deg,#aa351e 0%,#d8603d 52%,#eb8a67 100%);
  -webkit-background-clip:text;
  background-clip:text;
  color:transparent;
}

.lead{
  max-width:610px;
  margin:0;
  color:#665b54;
  font-size:clamp(17px,1.15vw,19px);
  line-height:1.72;
}

/* One unified information bar instead of three detached cards */
.eventGrid{
  display:grid;
  grid-template-columns:repeat(3,minmax(0,1fr));
  gap:0;
  max-width:610px;
  margin:28px 0 0;
  overflow:hidden;
  border:1px solid rgba(117,78,56,.12);
  border-radius:16px;
  background:rgba(255,255,255,.74);
  box-shadow:0 14px 34px rgba(80,47,29,.045);
  backdrop-filter:blur(12px);
}

.eventGrid div{
  min-height:78px;
  padding:14px 16px;
  border:0;
  border-radius:0;
  background:transparent;
  box-shadow:none;
}

.eventGrid div + div{
  border-left:1px solid rgba(117,78,56,.10);
}

.eventGrid span{
  display:block;
  margin-bottom:6px;
  color:#a1715b;
  font-size:9px;
  font-weight:900;
  letter-spacing:.14em;
}

.eventGrid strong{
  color:#211b18;
  font-size:14px;
  line-height:1.35;
}

.note{
  margin-top:15px;
  color:#7b6f68;
  font-size:12.5px;
  line-height:1.55;
}

.heroActions{
  display:flex;
  flex-wrap:wrap;
  gap:11px;
  margin-top:22px;
}

.heroActions .primary,
.heroActions .secondary{
  min-height:58px;
  border-radius:15px;
  padding:0 23px;
  font-size:14.5px;
  font-weight:850;
}

.heroActions .primary{
  border:1px solid rgba(126,38,19,.12);
  background:linear-gradient(135deg,#a9361f 0%,#d45e3b 58%,#e47652 100%);
  color:#fff;
  box-shadow:
    0 18px 38px rgba(177,61,31,.23),
    inset 0 1px 0 rgba(255,255,255,.24);
}

.heroActions .primary::after{
  content:"→";
  margin-left:8px;
  font-weight:900;
  transition:transform .2s ease;
}

.heroActions .primary:hover{
  filter:none;
  transform:translateY(-2px);
  box-shadow:
    0 22px 46px rgba(177,61,31,.29),
    inset 0 1px 0 rgba(255,255,255,.24);
}

.heroActions .primary:hover::after{
  transform:translateX(3px);
}

.heroActions .secondary{
  border:1px solid rgba(97,64,45,.14);
  background:rgba(255,255,255,.72);
  color:#302923;
  box-shadow:0 10px 26px rgba(72,43,27,.04);
}

.price{
  display:inline-flex;
  align-items:center;
  gap:9px;
  width:max-content;
  margin-top:15px;
  padding:8px 11px;
  border:1px solid rgba(114,75,53,.10);
  border-radius:11px;
  background:rgba(255,255,255,.60);
}

.price span,.price small{
  color:#84766e;
  font-size:10.5px;
}

.price strong{
  color:#d45e3b;
  font-size:15px;
}

/* -------------------------
   PROFESSIONAL CLAUDE VISUAL
   Uses existing live DOM only
   ------------------------- */

.visual{
  position:relative;
  width:min(100%,560px);
  min-height:530px;
  margin-inline:auto;
  display:grid;
  place-items:center;
  isolation:isolate;
}

/* Disable image-backed experiments from V6 */
.visual::before{
  content:"";
  position:absolute;
  z-index:-3;
  width:460px;
  height:460px;
  border-radius:50%;
  background:
    radial-gradient(circle at 50% 48%,rgba(230,112,72,.16) 0%,rgba(230,112,72,.07) 36%,transparent 68%);
  filter:blur(3px);
}

.visual::after{
  content:"";
  position:absolute;
  z-index:-2;
  width:470px;
  height:350px;
  border:1px solid rgba(221,111,76,.16);
  border-radius:50%;
  transform:rotate(-8deg);
  box-shadow:
    0 0 0 48px rgba(231,139,107,.035),
    0 0 0 96px rgba(231,139,107,.020);
}

.orbit,
.orbit2,
.orbit3{
  display:block !important;
  position:absolute;
  border-radius:50%;
  pointer-events:none;
}

.orbit{
  width:420px;
  height:420px;
  border:1px solid rgba(220,105,70,.16);
}

.orbit2{
  width:332px;
  height:332px;
  border:1px dashed rgba(220,105,70,.18);
  animation:v7Spin 28s linear infinite;
}

.orbit3{
  width:246px;
  height:246px;
  border:0;
  background:radial-gradient(circle,rgba(239,155,124,.13),transparent 68%);
}

.window{
  display:block !important;
  position:relative;
  z-index:2;
  width:min(440px,82%);
  padding:12px;
  border:1px solid rgba(111,70,48,.14);
  border-radius:24px;
  background:rgba(255,255,255,.84);
  box-shadow:
    0 30px 78px rgba(76,43,25,.16),
    0 4px 18px rgba(76,43,25,.05),
    inset 0 1px 0 rgba(255,255,255,.90);
  backdrop-filter:blur(18px);
  transform:translateY(-2px);
}

.windowTop{
  display:flex;
  align-items:center;
  justify-content:space-between;
  min-height:44px;
  padding:0 13px;
  border-bottom:1px solid rgba(121,78,55,.10);
  color:#251e1a;
  font-size:11px;
  font-weight:850;
}

.dots{
  display:flex;
  gap:5px;
}

.dots i{
  width:6px;
  height:6px;
  border-radius:50%;
  background:#ded0c8;
}

.prompt{
  margin:12px 4px 10px;
  padding:14px 15px;
  border:1px solid rgba(143,92,65,.08);
  border-radius:14px;
  background:linear-gradient(180deg,#fff5ef 0%,#fff1e8 100%);
  color:#5a4e47;
  font-size:12px;
  line-height:1.55;
}

.answer{
  margin:0 4px 4px;
  padding:18px;
  border:0;
  border-radius:17px;
  background:linear-gradient(145deg,#211b18 0%,#30231e 100%);
  box-shadow:inset 0 1px 0 rgba(255,255,255,.035);
}

.answer strong{
  color:#f7eee9;
  font-size:11px;
}

.answer b{
  display:block;
  height:7px;
  margin-top:10px;
  border-radius:999px;
  background:rgba(255,255,255,.16);
}

.answer b:nth-child(2){
  width:86%;
  background:linear-gradient(90deg,#f09a77,#d96a46);
}

.answer b:nth-child(3){width:70%;}
.answer b:nth-child(4){width:54%;}

.answerGrid{
  display:grid;
  grid-template-columns:repeat(3,minmax(0,1fr));
  gap:7px;
  margin-top:15px;
}

.answerGrid span{
  padding:8px 5px;
  border:1px solid rgba(255,255,255,.055);
  border-radius:9px;
  background:rgba(255,255,255,.06);
  color:#e9d6cc;
  text-align:center;
  font-size:9px;
  font-weight:750;
}

.chip{
  position:absolute;
  z-index:3;
  display:grid;
  place-items:center;
  width:auto;
  min-width:58px;
  height:auto;
  min-height:46px;
  padding:9px 11px;
  border:1px solid rgba(135,82,54,.12);
  border-radius:14px;
  background:rgba(255,255,255,.82);
  color:#a8482f;
  box-shadow:0 14px 34px rgba(79,45,27,.075);
  backdrop-filter:blur(12px);
  font-size:9px;
  font-weight:900;
  letter-spacing:.05em;
}

.chip1{top:70px;left:28px;}
.chip2{top:92px;right:18px;}
.chip3{right:54px;bottom:60px;}
.chip4{left:42px;bottom:82px;}

@keyframes v7Spin{
  to{transform:rotate(360deg);}
}

/* -------------- TABLET -------------- */
@media (max-width:1050px){
  .heroGrid{
    grid-template-columns:minmax(0,1fr) minmax(410px,.92fr);
    gap:38px;
  }
  .hero h1{font-size:clamp(52px,5.8vw,68px);}
  .visual{width:min(100%,510px);min-height:500px;}
  .window{width:min(410px,84%);}
}

@media (max-width:900px){
  .hero{padding:60px 0 64px;}
  .heroGrid{
    min-height:auto;
    grid-template-columns:1fr;
    gap:30px;
  }
  .heroGrid > div:first-child{
    max-width:760px;
    margin-inline:auto;
    text-align:center;
  }
  .hero h1{
    max-width:720px;
    margin-inline:auto;
    font-size:clamp(52px,8vw,70px);
  }
  .lead,.eventGrid{margin-left:auto;margin-right:auto;}
  .heroActions,.price{justify-content:center;margin-left:auto;margin-right:auto;}
  .visual{width:min(100%,540px);min-height:500px;}
}

/* -------------- MOBILE -------------- */
@media (max-width:640px){
  .container{width:min(100% - 28px,1160px);}
  .hero{padding:42px 0 50px;}
  .heroGrid{gap:20px;}
  .livePill{min-height:33px;padding:7px 10px;font-size:9.5px;}
  .hero h1{
    margin-top:18px;
    margin-bottom:14px;
    font-size:clamp(41px,11.8vw,54px);
    line-height:.985;
    letter-spacing:-.052em;
  }
  .lead{font-size:15.5px;line-height:1.62;}
  .eventGrid{
    margin-top:20px;
    border-radius:14px;
  }
  .eventGrid div{
    min-height:70px;
    padding:10px 5px;
    text-align:center;
  }
  .eventGrid span{font-size:7px;letter-spacing:.10em;}
  .eventGrid strong{font-size:10px;line-height:1.25;}
  .note{margin-top:12px;font-size:11px;}
  .heroActions{
    width:100%;
    margin-top:18px;
    flex-direction:column;
  }
  .heroActions .primary,
  .heroActions .secondary{
    width:100%;
    min-height:54px;
  }
  .price{display:none;}
  .visual{
    width:min(100%,460px);
    min-height:390px;
    margin-top:0;
  }
  .visual::before{width:330px;height:330px;}
  .visual::after{width:340px;height:250px;box-shadow:0 0 0 34px rgba(231,139,107,.03);}
  .orbit{width:320px;height:320px;}
  .orbit2{width:255px;height:255px;}
  .orbit3{width:190px;height:190px;}
  .window{width:min(360px,86%);padding:9px;border-radius:19px;}
  .windowTop{min-height:38px;padding-inline:10px;font-size:9px;}
  .prompt{padding:11px 12px;font-size:10px;}
  .answer{padding:14px;}
  .answer strong{font-size:9px;}
  .answerGrid span{font-size:7.5px;padding:7px 3px;}
  .chip{min-width:48px;min-height:38px;padding:7px 8px;border-radius:12px;font-size:7.5px;}
  .chip1{top:48px;left:7px;}
  .chip2{top:70px;right:2px;}
  .chip3{right:24px;bottom:40px;}
  .chip4{left:19px;bottom:58px;}
}

@media (max-width:430px){
  .hero{padding-top:36px;}
  .hero h1{font-size:clamp(38px,12vw,48px);}
  .visual{min-height:340px;}
  .visual::before{width:290px;height:290px;}
  .visual::after{width:295px;height:215px;}
  .orbit{width:280px;height:280px;}
  .orbit2{width:220px;height:220px;}
  .orbit3{width:160px;height:160px;}
  .window{width:min(320px,88%);}
}

@media (max-width:360px){
  .eventGrid strong{font-size:9px;}
  .visual{min-height:310px;}
}

@media (prefers-reduced-motion:reduce){
  .orbit2{animation:none !important;}
  .heroActions .primary::after{transition:none !important;}
}

/* CLAUDE HERO V7 PROFESSIONAL END */

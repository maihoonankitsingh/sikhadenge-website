name: Deploy Public Claude Once

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  completed:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'One-time Claude production deployment completed successfully on 2026-08-21; this workflow is disabled for production use.'

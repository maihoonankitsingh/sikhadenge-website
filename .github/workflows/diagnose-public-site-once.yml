name: Diagnose Public Site Once

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  completed:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Read-only post-deployment diagnostics completed successfully on 2026-08-21; this workflow is disabled for production use.'
